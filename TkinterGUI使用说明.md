# Tkinter桌面GUI使用说明

## 🎯 概述

TkinterGUI是网络安全信息收集工具的**桌面版本**，完全使用Python内置的tkinter库开发，**不依赖任何第三方Web框架**（如Flask、Django等）。

### ✅ 核心优势

- **100%兼容**: 使用Python标准库，无需额外安装依赖
- **桌面应用**: 原生窗口，无需浏览器
- **跨平台**: Windows、Linux、macOS都支持
- **稳定可靠**: 不依赖网络，不受防火墙影响
- **简单易用**: 图形化界面，点击操作

---

## 🚀 快速开始

### 启动方式

```bash
# 直接运行
python tkinter_gui.py

# 或双击运行（Windows）
双击: tkinter_gui.py
```

### 系统要求

- ✅ Python 3.6 或更高版本
- ✅ tkinter模块（Python标准库，通常预装）

**检查tkinter是否可用**:
```bash
python -c "import tkinter; print('tkinter可用')"
```

如果提示找不到tkinter：
- **Windows**: 重新安装Python，勾选"tcl/tk"选项
- **Linux**: `sudo apt-get install python3-tk`
- **Mac**: 通常预装，如果缺失请重新安装Python

---

## 📱 界面说明

启动后，你会看到一个桌面窗口，包含三个主要区域：

```
┌─────────────────────────────────────────────────────────┐
│  网络安全信息收集工具 (标题栏)                          │
├──────────────────┬──────────────────────────────────────┤
│                  │                                      │
│  左侧: 配置面板  │  右上: 进度面板                      │
│  ┌─────────────┐ │  ┌────────────────────────────────┐ │
│  │ 目标列表    │ │  │ 进度条 [==========>   65%]     │ │
│  │ (文本框)    │ │  │ 当前目标: example.com          │ │
│  └─────────────┘ │  │ 进度: 3/5                      │ │
│                  │  └────────────────────────────────┘ │
│  ┌─────────────┐ │                                      │
│  │ 模块选择    │ │  右下: 结果面板                      │
│  │ [x]子域名   │ │  ┌────────────────────────────────┐ │
│  │ [x]端口     │ │  │ + example.com                  │ │
│  │ [x]DNS      │ │  │   + DNS记录                    │ │
│  └─────────────┘ │  │     A记录: 93.184.216.34      │ │
│                  │  │   + WHOIS信息                  │ │
│  ┌─────────────┐ │  │     注册商: Example Registrar  │ │
│  │ 参数设置    │ │  └────────────────────────────────┘ │
│  │ 线程数: 30  │ │                                      │
│  │ 超时: 5     │ │                                      │
│  └─────────────┘ │                                      │
│                  │                                      │
│  [开始扫描]按钮  │                                      │
│                  │                                      │
│  ┌─────────────┐ │                                      │
│  │ 快速预设    │ │                                      │
│  │ 信息收集    │ │                                      │
│  └─────────────┘ │                                      │
└──────────────────┴──────────────────────────────────────┘
```

---

## 📝 使用步骤

### 步骤1: 输入扫描目标

在左侧"目标列表"文本框中输入要扫描的域名或IP地址：**每行一个**

**示例**: 
```
example.com
192.168.1.1
test.example.com
```

### 步骤2: 选择扫描模块

勾选需要使用的扫描模块：

- ✅ **子域名扫描**: 发现子域名
- ✅ **端口探测**: 扫描开放端口
- ✅ **服务识别**: 识别服务版本
- ✅ **DNS记录**: 获取DNS信息
- ✅ **WHOIS查询**: 域名注册信息
- ✅ **目录扫描**: 枚举网站目录
- ✅ **漏洞检测**: 检测安全漏洞
- ✅ **SSL分析**: 分析SSL证书
- ✅ **网络拓扑**: 路由追踪

**提示**: 可以选择多个模块，工具会按顺序执行扫描

### 步骤3: 设置扫描参数（可选）

- **线程数**: 并发扫描线程数量（默认30）
  - 内网环境: 可设置为50-100
  - 公网环境: 建议设置为10-30
- **超时时间**: 请求超时时间，秒（默认5）
  - 网络较差时可增加到10-15秒

### 步骤4: 开始扫描

点击"**开始扫描**"按钮

**注意**: 
- 扫描过程中可以点击"**停止扫描**"按钮中断
- 进度条会显示当前扫描进度
- 右上方面板会显示当前正在扫描的目标

### 步骤5: 查看结果

扫描结果会实时显示在右下角的树形列表中：

```
+ example.com
  + DNS记录
    A记录: 93.184.216.34
    MX记录: 1 mail.example.com
  + WHOIS信息
    注册商: Example Registrar
    创建日期: 1995-08-14
  + 子域名扫描
    发现 2 个子域名
      www.example.com
      api.example.com
```

### 步骤6: 保存报告

点击"**保存报告**"按钮，选择保存位置和格式：
- **HTML格式**: 包含详细结果和统计，可在浏览器中查看
- **JSON格式**: 结构化数据，可用于后续处理

---

## ⚡ 快速预设

为了方便使用，提供了三种快速预设配置：

### 1️⃣ 信息收集
**用途**: 收集目标基本信息
**包含模块**: 子域名扫描 + DNS记录 + WHOIS查询

**使用场景**:
- 了解目标的基本情况
- 资产发现和整理
- 信息收集阶段

### 2️⃣ 安全评估
**用途**: 检测目标安全状况
**包含模块**: 端口探测 + 服务识别 + 漏洞检测 + SSL分析

**使用场景**:
- 安全审计
- 漏洞评估
- 渗透测试前期

### 3️⃣ 网络探测
**用途**: 探测网络架构
**包含模块**: 端口探测 + 服务识别 + 网络拓扑

**使用场景**:
- 内网渗透
- 网络架构分析
- 资产测绘

---

## 💡 使用技巧

### 批量扫描
在目标列表中输入多个目标，每行一个：
```
example1.com
example2.com
192.168.1.1
192.168.1.2
```

工具会自动逐个扫描，并在结果中分组显示。

### 结果筛选
在结果树中：
- 点击 **+** 号展开子项
- 点击 **-** 号折叠子项
- 点击节点可以查看详细信息

### 保存和加载
- **保存报告**: 可以保存为HTML或JSON格式
- **清空结果**: 清除当前显示的所有结果
- **重新扫描**: 清空后可以输入新目标重新扫描

---

## 🔧 故障排除

### 问题1: GUI无法启动

**现象**: 运行`python tkinter_gui.py`后没有任何反应或报错

**解决方法**:

1. **检查Python版本**:
   ```bash
   python --version
   ```
   确保是3.6或更高版本

2. **检查tkinter模块**:
   ```bash
   python -c "import tkinter; print('ok')"
   ```
   如果报错，重新安装Python并勾选"tcl/tk"选项

3. **查看详细错误**:
   ```bash
   python -c "import tkinter_gui"
   ```
   查看具体错误信息

### 问题2: 扫描没有响应

**现象**: 点击"开始扫描"后，进度条不动

**可能原因**:
1. 目标格式不正确
2. 网络连接问题
3. 防火墙拦截

**解决方法**:
1. 检查目标格式（域名或IP）
2. 测试单个目标是否正常
3. 检查网络连接
4. 增加超时时间

### 问题3: 结果显示异常

**现象**: 结果树显示混乱或为空

**解决方法**:
1. 点击"清空结果"按钮
2. 重新开始扫描
3. 检查日志文件: `logs/tkinter-gui.log`

---

## 📊 性能建议

### 大规模扫描
- 建议分批扫描，每次不超过50个目标
- 可以适当增加线程数（50-100）
- 监控内存使用，避免资源耗尽

### 网络环境差
- 增加超时时间到10-15秒
- 减少线程数到10-20
- 使用更少的模块（2-3个）

### 系统资源
- 确保有足够的内存（建议2GB+）
- 关闭不必要的程序
- 使用64位Python版本

---

## 🔒 注意事项

### 合法使用
- 只对授权目标进行扫描
- 遵守当地法律法规
- 不要对生产环境进行高风险测试

### 结果保密
- 扫描结果包含敏感信息
- 妥善保管报告文件
- 及时清理不必要的日志

---

## 📚 相关文档

- **详细文档**: README.md
- **使用示例**: examples.txt
- **配置说明**: config.yaml
- **故障排除**: GUI故障排除指南.md

---

## 🆘 技术支持

如果仍然无法启动或使用，请提供：

1. **Python版本**: `python --version`
2. **错误信息**: 完整的错误输出
3. **操作系统**: Windows版本
4. **现象描述**: 详细的问题描述

---

## 🎉 总结

TkinterGUI提供了：
- ✅ 完全不依赖Flask的桌面应用
- ✅ 100%使用Python标准库
- ✅ 跨平台兼容（Windows/Linux/Mac）
- ✅ 简单易用的图形界面
- ✅ 完整的扫描功能

**享受使用桌面版网络安全信息收集工具！**
