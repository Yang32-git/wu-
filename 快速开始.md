# 网络安全信息收集工具 - 快速开始指南

## 🎉 工具已成功创建！

这个工具包含了所有你要求的功能模块，是一个全面的网络安全信息收集解决方案。

## 📋 已实现的模块

✅ **10个核心功能模块全部完成**：
1. **子域名扫描** - DNS区域传输、搜索引擎发现、暴力破解
2. **端口探测** - TCP/SYN扫描、Banner抓取
3. **服务识别** - 版本检测、CPE生成、OS指纹
4. **漏洞检测** - XSS、SQL注入、命令注入、安全头检查
5. **WHOIS查询** - 域名注册信息查询
6. **DNS记录获取** - A、AAAA、MX、NS、TXT等多种记录
7. **网站目录扫描** - 目录枚举、文件发现
8. **敏感信息检测** - 配置泄露、备份文件检测
9. **SSL证书分析** - 证书有效性、加密套件、协议版本
10. **网络拓扑探测** - 路由追踪、网络发现

## 🚀 快速开始

### 方法1: 启动Web GUI图形界面（推荐）🎨

这是最推荐的启动方式，提供友好的图形界面：

```bash
# 启动Web GUI（默认端口5000）
python main.py --gui

# 或启动后自动打开浏览器
start http://localhost:5000 && python main.py --gui

# 指定端口
python main.py --gui --gui-port 8080
```

GUI界面特点：
- ✅ 图形化操作，无需记忆命令
- ✅ 实时进度显示
- ✅ 结果可视化展示
- ✅ 一键下载报告
- ✅ 支持批量扫描
- ✅ 跨平台，浏览器访问

**GUI使用步骤**：
1. 在左侧输入目标（域名或IP，每行一个）
2. 选择扫描模块（勾选需要的功能）
3. 调整线程数和超时时间（或使用默认值）
4. 点击"开始扫描"
5. 查看实时进度和结果
6. 下载HTML报告

详细GUI使用说明请查看: **GUI使用说明.md**

### 方法2: 使用演示脚本（验证功能）
```bash
python demo.py
```
这个脚本展示了所有功能模块和使用示例。

### 方法3: 命令行扫描（需要修复依赖冲突）

当前由于Jinja2/MarkupSafe版本兼容性问题，完整扫描功能需要调整Python环境。

**临时解决方案：**
1. 创建虚拟环境（推荐）
2. 或者降级MarkupSafe: `pip install markupsafe==2.0.1`

**基本用法：**
```bash
# 扫描DNS和WHOIS信息
python main.py -t example.com -m dns,whois

# 扫描所有模块并生成报告
python main.py -t example.com -m all -o report.html

# 批量扫描
python main.py -f targets.txt -m all -o batch_report.html

# 启动API服务器
python main.py --api --port 8080
```

### 方法3: 使用Windows批处理脚本
双击运行 `启动工具.bat`，使用图形菜单操作。

## 📖 查看详细文档

- **README.md** - 完整的工具文档
- **examples.txt** - 详细的使用示例和场景
- **config.yaml** - 配置文件说明

## 🛠️ 安装依赖

如果遇到依赖问题，手动安装关键包：
```bash
pip install requests dnspython beautifulsoup4 lxml PyYAML urllib3 certifi
```

对于PDF报告生成功能：
```bash
pip install weasyprint
```

对于API服务器：
```bash
pip install flask flask-restful flask-cors
```

## 🎯 功能特点

- **高性能**：多线程并发，智能任务调度
- **易扩展**：模块化设计，易于添加新功能
- **跨平台**：支持Windows/Linux/macOS
- **报告丰富**：支持HTML/PDF/JSON格式
- **API支持**：RESTful接口，异步任务处理
- **日志完善**：详细的日志记录和错误处理

## 💡 使用建议

1. **小规模测试**：先用单个模块测试，如`python main.py -t example.com -m dns`
2. **调整线程数**：根据网络环境调整`--threads`参数（默认30）
3. **生成报告**：使用`-o report.html`参数保存扫描结果
4. **批量处理**：使用`-f targets.txt`批量扫描多个目标

## ⚠️ 法律声明

本工具仅供教育和授权测试使用。使用本工具扫描目标前，请确保已获得适当的授权。未经授权的网络扫描可能违反法律法规。

## 📞 技术支持

如遇到问题：
1. 查看 `logs/` 目录下的日志文件
2. 检查Python版本（需要3.6+）
3. 确保所有依赖已正确安装
4. 在测试环境中先行验证

---

**工具已准备就绪！** 根据你的Python环境选择合适的运行方式开始信息收集任务。
